import os
CLARIFAI = {'api_key':'', 'version':''} # Change the values to your api_key and version
IBM = {'api_key':'', 'version':''} # Change the values to your api_key and version
GOOGLE = os.pardir + os.sep + 'GoogleApi.json' # Change the path to the directory of your google api_key json file 
AMAZON = # %UserProfile%\.aws is the default path for credentials file and config file